package main

import (
	"fmt"
)

func main() {
	/*
		Comparing 2 Strings

			s1 := "aayush"
			s2 := "ankita"

			t := strings.Compare(s1, s2)
			if t < 0 {
				fmt.Print(s1, " < ", s2)
			} else if t == 0 {
				fmt.Print(s1, " = ", s2)
			} else {
				fmt.Print(s1, " > ", s2)
			}
			fmt.Println()
	*/

	fmt.Println()

	// Sorting Strings in ascending order

	// s := []string{"aayush", "ayush", "ankita", "nivi", "manish"}
	// sort.Strings(s)
	// fmt.Println(s)

	// arr := []int{1, 2, 3, 4, 5}
	// new_arr := change_arr(&arr)
	// fmt.Println(*new_arr)
}

// passing pointer to []int & return *[]int

// func change_arr(arr *[]int) *[]int {
//
// 	for i, v := range *arr {
// 		(*arr)[i] = v + 1
// 	}
// 	return arr
// }
